const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const ring=$('.cursor-ring'),dot=$('.cursor-dot');
window.addEventListener('mousemove',e=>{if(!ring)return;ring.style.left=e.clientX+'px';ring.style.top=e.clientY+'px';dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px'});
$$('a,button,.work-card,.case-node,.tool-window,.photo-frame').forEach(el=>{el.addEventListener('mouseenter',()=>{if(ring){ring.style.width='62px';ring.style.height='62px'}});el.addEventListener('mouseleave',()=>{if(ring){ring.style.width='38px';ring.style.height='38px'}})});
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.12});
$$('.about-intro,.about-bottom,.work-card,.case-title,.case-canvas,.case-meta div,.case-process div,.tool-window,.timeline article,.cert-grid div,.exp-grid article,.contact-links a').forEach((el,i)=>{el.classList.add('reveal');el.style.transitionDelay=Math.min(i*.045,.3)+'s';io.observe(el)});
const progress=$('.progress span'), scenes=$$('.scene'), chapterNo=$('.chapter-no'), chapterName=$('.chapter-name');
const names=['OPENING','POINT OF VIEW','ARCHIVE','CASE STUDY','TOOLS OF THE TRADE','THE JOURNEY','FIELD NOTES','FINAL FRAME'];
function update(){const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(scrollY/Math.max(1,max)*100)+'%';let best=0,dist=Infinity;scenes.forEach((s,i)=>{const d=Math.abs(s.getBoundingClientRect().top-innerHeight*.35);if(d<dist){dist=d;best=i}});if(chapterNo){chapterNo.textContent=String(best).padStart(2,'0');chapterName.textContent=names[best]||'OPENING'}}
addEventListener('scroll',update,{passive:true});update();
$$('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=$(a.getAttribute('href'));if(el){e.preventDefault();scrollTo({top:el.offsetTop-62,behavior:'smooth'})}}));
const device=$('.device');window.addEventListener('mousemove',e=>{if(!device||innerWidth<851)return;const x=e.clientX/innerWidth-.5,y=e.clientY/innerHeight-.5;device.style.transform=`rotateY(${-18+x*10}deg) rotateX(${7-y*7}deg) rotateZ(${-3+x*2}deg)`});
$$('.work-card').forEach(card=>{card.addEventListener('mousemove',e=>{if(innerWidth<851)return;const r=card.getBoundingClientRect(),x=e.clientX/r.left/r.width;const px=(e.clientX-r.left)/r.width-.5,py=(e.clientY-r.top)/r.height-.5;card.style.transform=`translateY(-8px) perspective(900px) rotateX(${py*-2.5}deg) rotateY(${px*3}deg)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
$$('.archive-tools button').forEach(btn=>btn.addEventListener('click',()=>{const f=btn.dataset.filter;$$('.archive-tools button').forEach(b=>b.classList.toggle('active',b===btn));$$('.work-card').forEach(card=>{card.classList.toggle('hide',f!=='all'&&!card.dataset.cat.split(' ').includes(f))})}));
